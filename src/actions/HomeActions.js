import alt from '../alt';
import $ from 'jquery';
import {
    assign,
    isEmpty
} from 'underscore';

class HomeActions {
  constructor() {
    this.generateActions(

    );
  }
}

export default alt.createActions(HomeActions);
